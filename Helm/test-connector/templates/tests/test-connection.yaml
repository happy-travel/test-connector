apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-connector.fullname" . }}-test-connection"
  labels:
    {{- include "test-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
